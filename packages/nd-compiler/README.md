# `@nodomx/nd-compiler`

Compiler utilities for NodomX `.nd` single-file components.

Supported blocks:

- `<template>`
- `<script>`
- `<style>`
- `<style scoped>`

Compile from CLI:

```bash
ndc ./src/App.nd --out ./src/App.nd.gen.mjs
```
