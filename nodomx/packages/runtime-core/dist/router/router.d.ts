import { Module } from "../module/module";
import { RenderedDom, RouteGuard, RouteLocation } from "../types";
import { Route } from "./route";
export declare class Router {
    private root;
    private rootModule;
    private basePath;
    currentPath: string;
    currentRoute: RouteLocation;
    private waitList;
    private onDefaultEnter?;
    private onDefaultLeave?;
    private startType;
    private routerMap;
    private beforeGuards;
    private afterGuards;
    constructor(basePath?: string, defaultEnter?: (module: Module, path: string) => void, defaultLeave?: (module: Module, path: string) => void);
    push(path: string): void;
    replace(path: string): void;
    go(path: string, replace?: boolean): void;
    resolve(path: string): RouteLocation;
    beforeEach(guard: RouteGuard): this;
    afterEach(hook: (to: RouteLocation, from?: RouteLocation) => unknown): this;
    addActiveDom(module: Module, dom: RenderedDom): void;
    registRouter(module: Module, dom: RenderedDom): void;
    activePath(path: string): void;
    getRoot(): Route;
    private load;
    private start;
    private resolveNavigation;
    private resolveTarget;
    private matchRoutes;
    private matchBranch;
    private matchRoute;
    private diffRoutes;
    private runGuards;
    private runAfterEach;
    private getModule;
    private syncRouteState;
    private setActiveDom;
    private handleRouteModule;
    private prepModuleDom;
    private getMatchedRoutes;
    private findLastModuleRoute;
    private resolveLoadedModule;
    private stripBasePath;
    private normalizeTargetPath;
}
